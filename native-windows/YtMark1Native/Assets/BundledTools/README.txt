This folder is reserved for bundled downloader binaries in the native
Windows rewrite. The goal is to ship yt-dlp and ffmpeg inside the app
install instead of requiring a separate user-managed dependency install.
